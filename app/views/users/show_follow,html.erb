<h3><%= @title %></h3>

<% if @users.any? %>
  <ul>
    <% @users.each do |user| %>
      <li>
        <%= link_to user.full_name, user_path(user) %>
      </li>
    <% end %>
  </ul>
  <%= will_paginate @users %>
<% else %>
  <p>Nema korisnika za prikaz.</p>
<% end %>
