<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "First Blog App" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <nav class="bg-gray-100 text-gray-900">
    <!-- Light Green Navbar -->
    <nav class="bg-green-400 p-4">
      <div class="container mx-auto">
        <div class="flex items-center justify-between">
          <!-- Home Link -->
          <div class="text-white text-lg font-semibold">
            <%= link_to "Home", root_path, class: "hover:text-gray-100" %>
          </div>
          <!-- User Links -->
          <div class="text-white flex items-center space-x-4">
            <% if user_signed_in? %>
              <%= link_to "Edit Profile", edit_user_registration_path, class: "hover:text-gray-100" %>
              <%= link_to "Sign Out", destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: "hover:text-gray-100" %>
            <% else %>
              <%= link_to "Sign Up", new_user_registration_path, class: "hover:text-gray-100" %>
              <%= link_to "Sign In", new_user_session_path, class: "hover:text-gray-100" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <main class="container mx-auto mt-16 px-5 py-10 bg-white shadow-md rounded-lg">
      <%= yield %>
    </main>
  </body>
</html>
